- 每个 API Group 遵循「types.go + register.go + install/ + vN/ + validation/」固定目录结构，并由 zz_generated.deepcopy.go 自动生成深拷贝
- 控制器子包统一采用 config/v1alpha1 配置 API + metrics/ + OWNERS 的三件套布局，并通过 injection.go 暴露注入点
- 平台相关实现按 _linux.go / _windows.go / _others.go 后缀拆分，由同一文件名在编译期选择