- Feature Gate 常量在 `kube_features.go` 中按字母序（大写优先）排列，每个条目使用 `// owner: @xxx` 与可选 `// kep:` 注释标注负责人与 KEP 链接。
- client-go 与 component-base 的 `PreRelease` 枚举通过显式 switch-case 映射转换，default 分支 panic 以保证两套常量集同步。
- 全局可写状态采用 `sync.Once` + `sync.Mutex` 保护的单例结构体（如 `capInstance`），对外暴露 `Get()` 返回值的只读拷贝。
- 测试友好的重置函数统一命名为 `ResetForTest`，并在文档中明确“仅从测试调用”。