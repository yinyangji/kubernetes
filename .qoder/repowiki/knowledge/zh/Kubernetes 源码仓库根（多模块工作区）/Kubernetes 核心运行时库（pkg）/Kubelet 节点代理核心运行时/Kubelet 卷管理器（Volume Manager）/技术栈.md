基于 Go 标准库 `sync.RWMutex` 保护缓存并发；使用 `k8s.io/apimachinery/pkg/util/wait.Until` 驱动 populator/reconciler 定时循环；通过 `k8s.io/kubernetes/pkg/volume/util/operationexecutor` 抽象异步卷操作执行器；SELinux 相关逻辑受 `features.SELinuxMountReadWriteOncePod` 特性门控制。