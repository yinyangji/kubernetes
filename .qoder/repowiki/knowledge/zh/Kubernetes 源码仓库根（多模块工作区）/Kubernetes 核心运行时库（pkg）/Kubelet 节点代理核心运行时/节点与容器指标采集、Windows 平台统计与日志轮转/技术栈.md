Linux 侧通过 CRI API (`k8s.io/cri-api`) 与 cAdvisor (`github.com/google/cadvisor/lib/model`) 拉取指标；Windows 侧使用 `github.com/JeffAshton/win_pdh` 访问系统 PDH 性能计数器，并通过 `syscall` 直接调用 `GetDiskFreeSpaceExW`；日志压缩使用标准库 `compress/gzip`，队列使用 `client-go/util/workqueue`。