CRI API (`k8s.io/cri-api/pkg/apis/runtime/v1`) 作为唯一镜像 I/O 通道；凭据解析使用 `credentialprovider` 插件体系（含 `ExternalCredentialProviderDockerKeyring`）；并发控制采用 Go channel token bucket；可选 OpenTelemetry tracing（`go.opentelemetry.io/otel/trace`）。