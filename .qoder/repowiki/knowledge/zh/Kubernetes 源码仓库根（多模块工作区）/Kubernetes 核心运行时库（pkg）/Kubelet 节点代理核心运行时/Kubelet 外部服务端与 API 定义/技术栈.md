HTTP 路由基于 `github.com/emicklei/go-restful/v3`；gRPC 服务使用原生 `google.golang.org/grpc`；OpenTelemetry 追踪通过 `otelrestful` 注入；Prometheus 指标通过 `component-base/metrics` 注册；配置对象使用 `+k8s:deepcopy-gen` / `+groupName=kubelet.config.k8s.io` 注解配合 code-generator 生成 deepcopy/conversion/defaults。