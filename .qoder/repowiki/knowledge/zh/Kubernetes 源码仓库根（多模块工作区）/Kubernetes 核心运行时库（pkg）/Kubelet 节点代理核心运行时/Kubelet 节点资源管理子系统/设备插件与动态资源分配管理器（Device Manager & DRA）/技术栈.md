gRPC over Unix Socket（`grpc.NewClient` + `insecure.NewCredentials`）；Device Plugin 使用 `k8s.io/kubelet/pkg/apis/deviceplugin/v1beta1`，DRA 使用 `k8s.io/kubelet/pkg/apis/dra/v1` 及 `k8s.io/dynamic-resource-allocation/resourceclaim`；检查点通过 `kubelet/checkpointmanager` 写入磁盘文件。