- 各策略型管理器（CPU/Memory/Topology/Device/DRA）均拆分为 policy_*.go + state/ 目录，state 下同时提供内存态与 checkpoint 持久化实现
- 所有可持久化的内部状态都通过 state.Checkpointer 接口抽象，并由 state_checkpoint.go/state_mem.go 分别提供磁盘与内存两种后端
- 平台差异通过 _linux/_windows/_unsupported 后缀文件拆分，Go build tag 控制编译分支而非运行时 if-else