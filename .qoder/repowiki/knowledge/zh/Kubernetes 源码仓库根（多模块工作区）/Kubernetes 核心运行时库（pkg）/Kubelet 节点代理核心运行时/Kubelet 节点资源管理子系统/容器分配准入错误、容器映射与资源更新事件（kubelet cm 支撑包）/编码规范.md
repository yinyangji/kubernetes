- 自定义错误通过实现统一的 `Error` 接口（`Error() string` + `Type() string`）并以字符串常量声明 `ErrorReason*` 标识，再由 `GetPodAdmitResult` 用 `errors.As` 判别后包装为 `lifecycle.PodAdmitResult`。
- 测试桩采用 mockery 的 testify 模板风格：每个方法配套 `*_Expecter`、`*_Call` 类型，并提供 `EXPECT().Method(...).Run(...).Return(...)` 与 `RunAndReturn` 两种断言路径。