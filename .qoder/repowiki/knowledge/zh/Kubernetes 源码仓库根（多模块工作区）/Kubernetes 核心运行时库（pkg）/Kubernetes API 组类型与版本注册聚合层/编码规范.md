- 每个 API Group 子包必须提供顶层 register.go 与 types.go，并在 v*/register.go 中将该版本加入 SchemeGroupVersion
- 对外公开版本位于 v* 子目录，并通过 zz_generated.conversion.go 与内部版本双向转换
- defaults.go 与 validations.go 由代码生成器产出，禁止手写默认值与校验逻辑
- 需要被 Scheme 注册的包通过 install/ 子包的 init() 副作用完成注册，而非显式调用