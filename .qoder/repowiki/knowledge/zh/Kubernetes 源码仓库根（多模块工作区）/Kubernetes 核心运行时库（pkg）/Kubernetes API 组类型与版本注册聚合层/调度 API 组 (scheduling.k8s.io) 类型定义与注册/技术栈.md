基于 `k8s.io/code-generator` 的 `conversion-gen`、`defaulter-gen`、`validation-gen` 生成 `zz_generated.conversion.go`、`zz_generated.defaults.go`、`zz_generated.validations.go`；使用 `+union` 注解表达 `PodGroupSchedulingPolicy`、`DisruptionMode`、`CompositePodGroupSchedulingPolicy` 等多态选择；通过 `+featureGate=...` 注解标记受特性门控控制的字段。