基于 k8s.io/code-generator 的注释驱动代码生成（`+k8s:deepcopy-gen`、`+genclient`、`+k8s:prerelease-lifecycle-gen`），使用 `k8s.io/apimachinery/pkg/runtime` 的 `SchemeBuilder` 进行版本注册，并通过 `k8s.io/apimachinery/pkg/conversion` 的手动 `Convert_*` 函数覆盖自动生成的转换逻辑。