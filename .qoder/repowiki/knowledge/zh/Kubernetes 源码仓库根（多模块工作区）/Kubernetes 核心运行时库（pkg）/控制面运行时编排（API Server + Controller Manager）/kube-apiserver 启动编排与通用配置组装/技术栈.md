基于 `k8s.io/apiserver/pkg/server` 的 generic apiserver 框架；认证链使用 `k8s.io/apiserver/plugin/pkg/authenticator/token/oidc` 支持 RS256/ES256/PS256 等 JOSE 算法；Leader Election 同时支持 legacy `coordination.k8s.io/v1beta1` Lease 与新式 `LeaseCandidate`（CoordinatedLeaderElection feature gate）。