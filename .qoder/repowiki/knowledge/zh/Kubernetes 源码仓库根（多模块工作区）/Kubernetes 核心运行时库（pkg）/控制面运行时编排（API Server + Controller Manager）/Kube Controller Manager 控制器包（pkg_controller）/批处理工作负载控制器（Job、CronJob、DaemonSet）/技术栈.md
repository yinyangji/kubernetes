Go controller-runtime 风格的手写控制器：client-go informers/listers + TypedRateLimitingQueue + cache.Indexer；通过 apiserver feature gate 动态启用一致性存储（`consistencyutil.ConsistencyStore`）；Prometheus 指标通过各自 `metrics.Register()` 注册。