基于 client-go informer/listener/workqueue 的标准 KCM 控制器骨架；指标通过 `k8s.io/component-base/metrics` 的 `StableCollector` 自定义采集；CSI 迁移与 in-tree 插件解析复用 `pkg/controller/volume/common`、`pkg/volume/util`、`csimigration.PluginManager`。