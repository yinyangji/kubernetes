- API 包统一在 `doc.go` 顶部添加 `+k8s:deepcopy-gen=package` 注释，由 code-generator 批量生成 `zz_generated.deepcopy.go`。
- 配置结构体通过嵌入其他包的 `*Configuration` 字段来组合，而非自行重新声明字段，保持单一来源。
- 可插拔组件通过接口（如 `ConsistencyStore`、`LastSyncRVGetter`）定义边界，并提供 `NewXxx` 构造函数返回真实或 Noop 实现。
- 测试辅助对象遵循 `Fake*` 命名约定并实现对应 client-go interface，内部用 `sync.Mutex` 保护可变状态并在方法退出前递增 `RequestCount`。