基于 client-go informer/listener/workqueue 标准控制器框架；使用 `k8s.io/utils/clock` 抽象时间以支持测试注入；StatefulSet 使用 `uutils/lru` 缓存 revision 相等性判断；通过 `utilfeature.DefaultFeatureGate` 开关 `StaleControllerConsistency*` 特性启用一致性追踪。