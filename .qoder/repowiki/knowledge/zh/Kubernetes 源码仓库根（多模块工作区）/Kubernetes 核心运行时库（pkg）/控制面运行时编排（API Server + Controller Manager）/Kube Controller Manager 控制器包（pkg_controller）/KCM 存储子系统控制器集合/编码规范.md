- 每个控制器以独立子包形式组织，内部包含 controller.go + *_controller_test.go 以及可选的 config/metrics/testing 子目录
- config 子包遵循 v1alpha1 conversion + defaults + register + zz_generated.deepcopy 的标准生成模式
- 控制器均基于 Informer/WorkQueue 模式实现，通过 enqueue 对象 key 到队列触发 reconcile