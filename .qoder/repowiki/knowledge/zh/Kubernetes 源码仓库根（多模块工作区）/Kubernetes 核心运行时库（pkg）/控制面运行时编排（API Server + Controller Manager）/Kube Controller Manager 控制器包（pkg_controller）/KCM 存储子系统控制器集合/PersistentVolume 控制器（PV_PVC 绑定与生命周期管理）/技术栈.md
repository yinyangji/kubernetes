Go + client-go Informer/Lister/WorkQueue 控制器骨架；使用 `k8s.io/component-helpers/storage/volume` 做 PV/PVC 字段校验与注解操作；CSI 迁移借助 `k8s.io/csi-translation-lib` 与 `pkg/volume/csimigration`；指标通过 `k8s.io/component-base/metrics` 注册。