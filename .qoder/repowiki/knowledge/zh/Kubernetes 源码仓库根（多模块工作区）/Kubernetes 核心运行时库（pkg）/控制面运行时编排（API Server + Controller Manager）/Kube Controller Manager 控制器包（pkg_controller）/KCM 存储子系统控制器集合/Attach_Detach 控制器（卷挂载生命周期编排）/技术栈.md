基于 client-go informer/listener/workqueue 的 controller-runtime 风格循环；CSI in-tree 迁移通过 `csimigration.PluginManager` + `csitrans.New()` 完成 intree→CSI spec 转换；操作执行委托给 `pkg/volume/util/operationexecutor` 中的指数退避队列。