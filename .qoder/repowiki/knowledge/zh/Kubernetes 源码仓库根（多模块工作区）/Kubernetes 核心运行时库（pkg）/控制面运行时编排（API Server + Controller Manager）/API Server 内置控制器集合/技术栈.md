基于 client-go informer/listers/workqueue 构建；Leader 选举同时使用 `k8s.io/client-go/tools/leaderelection/resourcelock`（leases 资源锁）和 `coordination.k8s.io/v1beta1.LeaseCandidate` 配合 `github.com/blang/semver/v4` 做版本比较；事件记录使用 `k8s.io/client-go/tools/record`。