- 对外部 API 调用统一采用 for 循环 + `numRetries=3` 的重试模式（如 `addNodeToNodeGroup`、`RemoveNodeFromNodeGroup`）。
- 共享状态（`nodeGroupQueueSize`、`nodesToDelete`）均配套独立的 `sync.Mutex` 字段并在读写前加锁，避免与 goroutine 并发冲突。
- 通过 `labels.SelectorFromSet(labels.Set{...})` 构造 label 选择器来查询属于某 nodegroup 的 Pod/RC，而非手写字符串拼接。