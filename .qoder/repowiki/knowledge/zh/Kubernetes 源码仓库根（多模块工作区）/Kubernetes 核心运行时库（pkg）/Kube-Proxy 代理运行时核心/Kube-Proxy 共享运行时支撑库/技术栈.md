Go + Kubernetes code-generator（deepcopy-gen、conversion-gen、defaults-gen）；Prometheus 指标通过 `k8s.io/component-base/metrics` 的 `legacyregistry.MustRegister` 注册；conntrack 操作依赖 `github.com/vishvananda/netlink` 与 `golang.org/x/sys/unix`；NFTables/iPTables 相关指标通过 `k8s.io/kubernetes/pkg/proxy/util/nfacct` 读取 netfilter accounting 计数器。