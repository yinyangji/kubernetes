- 条件判断函数统一以 `(event watch.Event) (bool, error)` 签名暴露，将删除事件转换为 `NewNotFound` 错误，将终态转换为专用哨兵错误，非目标类型或中间状态返回 `(false, nil)` 表示继续等待。
- 集成测试使用 `t.Run(tc.name, ...)` 表格驱动组织用例，并通过 `httptest.NewUnstartedServer` + `atomic.Uint64` 计数器断言请求次数、URL Path 与 Content-Type。