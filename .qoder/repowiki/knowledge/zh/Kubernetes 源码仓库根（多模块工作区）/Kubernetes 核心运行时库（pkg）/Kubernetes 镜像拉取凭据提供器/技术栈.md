Go；JSON 序列化使用 `k8s.io/apimachinery/pkg/runtime` 的严格 codec（含 v1/v1alpha1/v1beta1 多 APIVersion）；进程外插件通过 `os/exec` 以 stdin/stdout 传输；并发保护使用 `golang.org/x/sync/singleflight`；指标暴露走 `k8s.io/component-base/metrics`（在 plugin/metrics.go）。