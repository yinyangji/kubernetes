基于 `k8s.io/apiserver/pkg/registry/generic/registry.Store` 作为通用存储基座，通过 `generic.RESTOptionsGetter` 注入 etcd 后端；服务类资源使用自研的 bitmap/CIDR 分配器（`core/service/ipallocator`、`portallocator`）并通过 PostStartHook 启动 repair 控制器。