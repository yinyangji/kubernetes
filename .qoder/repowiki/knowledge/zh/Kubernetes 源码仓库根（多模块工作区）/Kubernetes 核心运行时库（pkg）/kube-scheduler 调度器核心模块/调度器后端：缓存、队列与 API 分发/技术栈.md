Go 泛型堆 (`container/heap`) + `kube-scheduler/framework` 扩展点（`PreEnqueuePlugin`、`QueueingHintFn`、`APICallRelevances`）；通过 `utilfeature.DefaultFeatureGate` 开关 GenericWorkload / CompositePodGroup / SchedulerAsyncAPICalls / SchedulerPopFromBackoffQ / OpportunisticBatching。