基于 `k8s.io/kube-scheduler/framework` 插件框架；通过 `client-go/informers` 监听 API Server 事件；Extender 使用 `extenderv1` JSON over HTTP 协议；可选启用 `DynamicResourceAllocation`、`CompositePodGroup`、`GenericWorkload`、`SchedulerAsyncAPICalls` 等 feature gate 控制分支。