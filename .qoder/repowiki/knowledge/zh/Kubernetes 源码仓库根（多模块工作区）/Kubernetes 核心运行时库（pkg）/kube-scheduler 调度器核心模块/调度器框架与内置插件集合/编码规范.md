- 每个插件以独立子目录组织，包含 `<plugin>.go` 主文件与同名 `_test.go`，并通过 `plugins/registry.go` 中的 `RegisterDefaultPlugins` 统一注册
- 插件对外暴露的 Lister/Manager 接口集中在 `autoscaler_contract/` 中声明，下游仅依赖该契约而非具体实现
- 跨插件共享的可变上下文统一通过 `CycleState` 的 Key 存取，禁止直接传递指针或全局变量