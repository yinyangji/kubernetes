- 每个 APICall 实现以独立文件组织，并提供同名 `New*Call` 构造函数返回指针类型。
- 通过全局 `Relevances` 与 `Implementations` 变量集中注册新类型，而非在 init() 内隐式注册。
- 对外暴露的可变全局映射（Relevances/Implementations）均附带注释说明可由 out-of-tree plugin 在 init() 中替换。
- Execute/Sync/Merge 等方法中对传入对象进行 DeepCopy 后再修改，避免污染原始对象。