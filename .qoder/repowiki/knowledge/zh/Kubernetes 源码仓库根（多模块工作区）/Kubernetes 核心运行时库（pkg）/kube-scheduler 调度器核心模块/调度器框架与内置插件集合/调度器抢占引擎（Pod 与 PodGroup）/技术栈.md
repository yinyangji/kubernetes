基于 `k8s.io/kube-scheduler/framework` 的 `fwk.Handle`/`SharedLister`/`CycleState` 访问调度快照；通过 `feature.Features` 开关 `EnableAsyncPreemption` 与 `EnableGenericWorkload` 切换行为；使用 `k8s.io/kubernetes/pkg/scheduler/framework/parallelize` 做并发干跑与 victim 删除。