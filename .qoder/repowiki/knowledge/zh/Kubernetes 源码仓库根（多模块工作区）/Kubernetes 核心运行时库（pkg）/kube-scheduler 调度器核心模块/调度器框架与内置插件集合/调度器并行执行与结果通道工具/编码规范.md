- 对外并发原语以 struct + receiver 形式暴露（如 `Parallelizer`、`ResultChannel[T]`），并通过 `New*` 构造函数初始化，避免暴露裸字段。
- 非阻塞 I/O 一律采用 `select { case ...: default }` 模式，确保 Send/Receive 不会阻塞调用方 goroutine。
- 资源生命周期在函数入口集中分配、`defer` 中统一释放（如 `Until` 中对 `goroutinesMetric.Add` 的正负成对操作）。