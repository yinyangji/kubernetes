- 对外 API 以 `Interface` + 结构体实现分离，构造器返回接口类型（如 `NewNodeInfoManager` 返回 `Interface`）。
- 对共享资源（Node、CSINode）的修改统一采用 `sync.Mutex` 串行化 + `wait.ExponentialBackoff` 重试，并将多次错误聚合成 `utilerrors.NewAggregate` 返回。
- Node 字段变更通过可组合的 `nodeUpdateFunc(*v1.Node) (*v1.Node, bool, error)` 闭包链式应用，先 DeepCopy 再比较是否 needUpdate，避免无谓 Patch。
- 所有对 etcd 的 Get/Patch/Update/Delete 调用均传入 `context.TODO()` 并显式包装错误上下文（如 `error updating Node object with CSI driver node info`），便于上层定位来源。
- 生命周期钩子（Add/Update/Delete）遵循空指针与类型断言快速返回的模式，仅在关键路径记录 `klog.V(4)/V(7)` 级别日志。