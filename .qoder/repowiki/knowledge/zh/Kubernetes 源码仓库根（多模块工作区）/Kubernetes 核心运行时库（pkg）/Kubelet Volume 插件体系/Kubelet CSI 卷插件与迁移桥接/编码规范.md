- 所有对外暴露的组件均实现 `volume.VolumePlugin` 接口，由 `ProbeVolumePlugins()` 统一注册到 Kubelet VolumePluginMgr。
- 对 CSI Driver 的 gRPC 调用统一封装在 `newCsiDriverClient` / `csi_client.go` 中，并通过 `context.WithTimeout(csiTimeout)` 强制超时。
- 驱动注册回调使用文件级全局变量（`csiDrivers`、`nim`、`csiNodeUpdaterVar`）作为共享状态，避免显式依赖注入。