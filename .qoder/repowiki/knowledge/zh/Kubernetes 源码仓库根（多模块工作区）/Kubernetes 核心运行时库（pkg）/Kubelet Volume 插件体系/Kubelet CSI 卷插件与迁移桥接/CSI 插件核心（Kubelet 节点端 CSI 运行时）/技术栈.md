CSI 节点侧 gRPC 客户端基于 `github.com/container-storage-interface/spec/lib/go/csi` v1.x，通过 `google.golang.org/grpc` 以 unix socket + `insecure.NewCredentials()` 直连 driver registrar 暴露的端点；能力探测走 `NodeGetCapabilities` 返回的 `NodeServiceCapability_RPC_*` 枚举。