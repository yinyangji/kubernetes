基于 `github.com/container-storage-interface/spec/lib/go/csi` gRPC stub 生成接口；使用 `google.golang.org/grpc/codes` 构造标准 gRPC 错误码（如 `DeadlineExceeded`、`Internal`）；借助 `k8s.io/client-go/informers` 的 `SharedInformerFactory` 驱动 CSIDriver informer 同步。