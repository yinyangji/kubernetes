基于 Linux 项目配额（xfs_quota / lsattr）；通过 `golang.org/x/sys/unix.Flock` 对 `/etc/projects`、`/etc/projid` 加锁；使用 `k8s.io/mount-utils` 的 `mount.Interface` 抽象挂载点检测；配额大小上限由 `syscall.Statfs_t.Type` 魔数区分 XFS(0x58465342) 与 ext4(0xef53)。