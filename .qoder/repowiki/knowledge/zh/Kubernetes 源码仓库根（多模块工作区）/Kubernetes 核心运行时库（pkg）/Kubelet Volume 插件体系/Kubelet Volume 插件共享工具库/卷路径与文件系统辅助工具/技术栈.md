Go 构建标签 (`//go:build linux|windows|unsupported`) 进行平台分发；Linux 端使用 `golang.org/x/sys/unix` 直接调用 `statfs`/`stat` 获取 inode 与 major/minor，通过 `losetup` 外部命令管理 loop 设备；Windows 端通过 `kernel32.dll` 的 `GetDiskFreeSpaceExW` 获取磁盘空间；bind mount 统一经由 `k8s.io/mount-utils` 的 `SafeFormatAndMount`。