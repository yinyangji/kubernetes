通过设置 `KUBERNETES_PROVIDER=gce|kubemark|pre-existing|...` 并执行 `./cluster/kube-up.sh` 拉起集群；Windows 节点需额外设置 `WINDOWS_NODE_OS_DISTRIBUTION=win2019`；清理集群使用 `./cluster/kube-down.sh`；验证集群健康度使用 `./cluster/validate-cluster.sh`。