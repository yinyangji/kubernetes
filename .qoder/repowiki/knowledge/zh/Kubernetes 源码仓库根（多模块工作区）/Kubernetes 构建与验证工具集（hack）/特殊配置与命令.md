所有脚本必须在仓库根目录执行；开发常用入口为 `make verify`（等价于 `hack/verify-all.sh`）和 `make update`（等价于 `hack/update-all.sh`）；单元测试通过 `make test` 或 `hack/make-rules/test.sh` 触发，默认启用 `-race` 与缓存变更检测；CI 场景通过 `jenkins/*-dockerized.sh` 在容器中运行。