Bash + GNU Make 为主；Go 工具链通过 GOTOOLCHAIN 管理（hack/tools 独立 go.work）；测试报告使用 gotestsum + JUnit XML；lint 配置集中在 golangci.yaml / golangci-hints.yaml；protobuf 生成由 `_update-generated-protobuf-dockerized.sh` 驱动。