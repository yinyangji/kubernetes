`genfeaturegates` 需要确保 `k8s.io/kubernetes/pkg/features`、`k8s.io/apiserver/pkg/features`、`k8s.io/apiextensions-apiserver/pkg/features` 三个包的 feature 定义已编译进二进制，否则无法枚举特性；`fieldnamedocscheck` 和 `genswaggertypedocs` 均需传入 `-s` 指向包含 k8s 类型注释的 Go 源文件路径。