- 每个子目录是一个独立的 `package main` 可执行工具，职责单一且彼此无共享代码。
- CLI 入口统一先 `flag.Parse()`，再对必填参数（如 `-s`）做空值检查并通过 `klog.Fatalf` 或 `os.Exit(1)` 退出。
- 错误信息优先写入 `os.Stderr`，成功时返回码为 0，失败时显式 `os.Exit(1)`，便于在 CI 中直接作为断言步骤。