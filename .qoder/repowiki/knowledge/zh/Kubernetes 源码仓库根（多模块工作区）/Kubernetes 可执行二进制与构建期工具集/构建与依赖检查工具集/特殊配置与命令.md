各工具均为独立可执行文件，典型用法：
- `clicheck`：直接运行，无需参数。
- `dependencycheck <json-dep-file>`：需配合 `go list -mod=vendor -test -deps -json ./vendor/...` 生成输入，并通过 `-restrict`、可选 `-exclude` 指定正则模式。
- `dependencyverifier dependencies.json`：需在仓库根目录运行，依赖 `vendor/modules.txt` 存在且 `dependencies.json` 包含匹配的 spec/status 结构。