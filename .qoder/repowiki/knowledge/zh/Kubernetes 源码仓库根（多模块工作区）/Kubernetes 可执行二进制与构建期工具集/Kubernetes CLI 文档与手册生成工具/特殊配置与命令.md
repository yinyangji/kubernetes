每个工具以两个位置参数运行：`[output directory] [module]`（gendocs 仅接受一个目录参数）。module 取值限定为 `kube-apiserver`、`kube-controller-manager`、`kube-proxy`、`kube-scheduler`、`kubelet`、`kubeadm`（genman 还多一个 `kubectl`），不在 switch 中会直接退出。