- cobra 命令采用 `NewXxxCommand() *cobra.Command` + `run(ctx, config) error` 的固定拆分模式，flags 集中在结构体方法 `addFlags` 中注册。
- 对外部组件的初始化统一包装为 `Failed to ..., error: %w. Exiting` 的错误格式，便于快速定位失败阶段。
- 对已废弃 flag 保留字段并在 `addFlags` 中显式调用 `fs.MarkDeprecated` 标注，而非直接删除。