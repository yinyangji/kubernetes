Go；CRI 客户端使用 `k8s.io/cri-api/pkg/apis/runtime/v1` gRPC；特征门控基于 `k8s.io/component-base/featuregate`；配置反序列化走 `kubeadm/apis/kubeadm/scheme` 与 `sigs.k8s.io/yaml`；静态 Pod 补丁使用 `github.com/pmezard/go-difflib/difflib` 做 diff 输出。