基于 Go build tag 的多平台分发；系统级验证委托给 `k8s.io/system-validators` 的 `KernelValidator`、`OSValidator`、`CgroupsValidator`；CRI 交互通过 `utilruntime.ContainerRuntime` 抽象；etcd 外部版本检查使用带重试与 TLS 配置的 HTTP 客户端轮询 `/version` 端点。