本模块按可执行二进制划分边界：cmd/kubectl 与 cmd/kubectl-convert 各自以 main.go 启动独立的 cobra 命令树，面向用户直接调用；cmd/kubeadm 则作为集群初始化/升级/加入等运维流程的编排器，内部通过 app/cmd 下的子命令 + app/phases 工作流阶段引擎组合出 init/join/reset/upgrade 等高层操作。三者之间无运行时依赖，仅共享上层 pkg/ 中的通用库（如 apimachinery、client-go），由各自的 go.mod 单独管理版本。